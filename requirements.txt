# Framework core
Django==5.2.8
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1

# Async / realtime
channels==4.3.2
channels-redis==4.3.0
asgiref>=3.7.2

# Background tasks / workers
celery==5.5.3
redis>=4.5.0

# Database (Postgres common choice)
psycopg2-binary>=2.9.7

# Static / media / images
Pillow==12.0.0
whitenoise>=6.0.0

# Deployment / WSGI/ASGI servers
gunicorn>=20.1.0
daphne>=4.0.0

# Config / env
django-environ>=0.10.0
python-dotenv>=1.0.0

# API docs, filtering & helpers
drf-yasg>=1.21.11
django-filter>=23.2

# Forms / frontend helpers
django-crispy-forms>=2.1.0

# Dev / debugging / productivity
django-debug-toolbar>=4.0
django-extensions>=4.0
pytest>=7.4.0
pytest-django>=4.5.2
coverage>=7.2.0

# Optional / nice-to-have
django-rest-knox>=4.2.0      # alternativs to JWT if you prefer tokens
httpx>=0.25.0                # async HTTP client (useful for webhooks, microservices)